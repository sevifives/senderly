h1= title

table
  thead
    tr
      td Metric
      td Value
  tbody
    for val, key in data
      if (key !== 'domains' || key !== 'ip_auth' || key !== ss_trend)
        tr
          td #{key}
          td #{val}